import { Mesh, WonderlandEngine } from '@wonderlandengine/api';
export declare function nineSlice(engine: WonderlandEngine, width: number, height: number, borderSize: number, borderTextureSize: number, oldMesh?: Mesh | null): Mesh;
